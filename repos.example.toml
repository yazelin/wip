# Copy to ~/.config/wip/repos.toml and edit.
# Curated list of repos to track - keep it to what's actively in flight.
# `~` is expanded to $HOME. Absolute paths also work.
repos = [
  "~/projects/backend-api",
  "~/projects/web-app",
  "~/projects/cli-tool",
  "~/work/service-a",
  "~/work/service-b",
]
